services:
  djangox509:
    build: 
      context: .
      dockerfile: getEAP_TLS_Dockerfile
    volumes:
      - .:/djangox509
    ports:
      - "8000:8000"
    working_dir: /djangox509/getEAP_TLS
    command: ["./docker-entrypoint.sh"]
